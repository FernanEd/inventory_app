extends layout 

block content 
  h1=title  
  p All items from all categories.

  hr.xsm

  h2 Related items
  if items.length > 0
    .items-wraper.xsm
      for item in items 
        .items-item 
          img(src=(item.imgUrl), alt="Item image." class='item-img-sm')
          .items-item-content
            h3 #[a(href=(item.url)) #{item.name}]
            p In stock: #{item.stock}
            p #[strong $#{item.price}.00] 
            
  else
    p This category has no items.